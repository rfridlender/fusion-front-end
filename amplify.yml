version: 1
frontend:
  phases:
    preBuild:
      commands:
        - env | grep -e NUXT_
        - corepack enable
        - npx --yes nypm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
