version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - npx --yes nypm i
    build:
      commands:
        - env | grep -e NUXT_PUBLIC_ >> .env
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
